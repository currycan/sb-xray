# sb-xray 回国 reverse bridge 节点清单（多公网高可用）
#
# 用法：cp nodes.list.example nodes.list && vi nodes.list
#   openwrt-init.sh 会优先读取脚本同目录的 nodes.list（或 NODES_FILE 指定路径），
#   装入 /etc/cn-exit/nodes.list 供 cn-bridge 拨号与 OpenClash 解耦遍历。
#
# 每行一台 VPS：<名> <FQDN> <token>，空格分隔；# 注释与空行忽略。
#   名    任意短标识，cn-bridge up/down 用它，建议用 VPS 裸名（如 dc99）
#   FQDN  VPS 对外域名（不带 https://），bridge 拨向它、OpenClash 对它解耦
#   token 该 VPS `docker exec sb-xray show` 输出里订阅链接 ?token= 后那段
#
# 热备（常驻拨通）在 config.env 的 BRIDGE_HOT 指定，例：BRIDGE_HOT=dc99,jp
# 未列入热备的即冷备：平时不拨、仅靠 socks5 腿回国，故障时 cn-bridge up <名> 顶上。
#
# 名        域名                       token
# jp        jp.example.com             <jp_token>
# dc99      dc99.example.com           <dc99_token>
# dc99-3    dc99-3.example.com         <dc99-3_token>
# cn2       cn2.example.com            <cn2_token>
# ...（多台节点，按需增删）
